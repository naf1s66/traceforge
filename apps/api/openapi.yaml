openapi: 3.0.3
info:
  title: TraceForge API
  version: v1
servers:
  - url: /api/traceforge/v1
paths:
  /health:
    get:
      summary: Health check
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
